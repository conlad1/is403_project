<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= pageTitle %> | Productivity Dashboard</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <script src="https://cdn.tailwindcss.com"></script>
  <!-- Chart.js CDN -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body class="bg-gray-50">

<div class="flex h-screen">
  <!-- Sidebar -->
  <%- include('partials/sidebar', { current: 'dashboard' }) %>

  <!-- Main content -->
  <main class="flex-1 overflow-y-auto p-8 space-y-8">
    <header>
      <h1 class="text-2xl font-semibold text-gray-900 mb-2">Your Performance Dashboard</h1>
      <p class="text-gray-600">Discover your peak productivity patterns</p>
    </header>

    <!-- Stats grid -->
    <section class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
      <% stats.forEach(function(stat, index) { %>
        <div class="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
          <p class="text-gray-600 text-sm mb-1"><%= stat.label %></p>
          <p class="text-gray-900 text-xl font-semibold"><%= stat.value %></p>
        </div>
      <% }) %>
    </section>

    <!-- Charts -->
    <section class="grid grid-cols-1 lg:grid-cols-2 gap-6">
      <!-- Hourly effectiveness -->
      <div class="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
        <h2 class="text-gray-900 text-lg font-semibold mb-4">Effectiveness by Hour</h2>
        <canvas id="hourlyChart"></canvas>
      </div>

      <!-- Weekly patterns -->
      <div class="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
        <h2 class="text-gray-900 text-lg font-semibold mb-4">Weekly Effectiveness</h2>
        <canvas id="weeklyChart"></canvas>
      </div>
    </section>

    <!-- Insights -->
    <section class="bg-gradient-to-r from-indigo-500 to-purple-600 rounded-xl p-6 text-white">
      <h2 class="text-lg font-semibold mb-4">💡 Insights & Recommendations</h2>
      <ul class="space-y-3 text-sm">
        <li class="flex gap-3">
          <span class="text-indigo-200">•</span>
          <span>Your peak performance is between 9 AM - 11 AM. Schedule your most important tasks during this window.</span>
        </li>
        <li class="flex gap-3">
          <span class="text-indigo-200">•</span>
          <span>Thursday is your most productive day. Consider tackling complex projects on Thursdays.</span>
        </li>
        <li class="flex gap-3">
          <span class="text-indigo-200">•</span>
          <span>Your effectiveness dips after lunch (1–2 PM). Plan a short break or handle lighter tasks during this time.</span>
        </li>
      </ul>
    </section>
  </main>
</div>

<!-- Pass data to JS -->
<script>
  window.DASHBOARD_DATA = {
    hourlyData: <%- JSON.stringify(hourlyData) %>,
    weeklyData: <%- JSON.stringify(weeklyData) %>
  };
</script>
<script src="/js/dashboard.js"></script>

</body>
</html>