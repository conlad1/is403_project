<div class="w-64 bg-slate-950 border-r border-slate-800 px-5 py-6 flex flex-col">
  <div class="flex items-center gap-2 mb-8">
    <div class="h-8 w-8 rounded-xl bg-gradient-to-tr from-blue-500 to-cyan-400 flex items-center justify-center text-xs font-bold text-slate-950">
      FT
    </div>
    <div>
      <div class="text-sm font-semibold text-slate-100">FlowTrack</div>
      <div class="text-xs text-slate-500">See when you work best</div>
    </div>
  </div>

  <nav class="flex flex-col gap-1 text-sm">
    <a href="/dashboard"
       class="flex items-center px-3 py-2 rounded-lg font-medium
       <%= (current === 'dashboard'
            ? 'bg-slate-800 text-blue-400'
            : 'text-slate-400 hover:bg-slate-800 hover:text-slate-100') %>">
      <span>Dashboard</span>
    </a>

    <a href="/log-activity"
       class="flex items-center px-3 py-2 rounded-lg font-medium
       <%= (current === 'log-activity'
            ? 'bg-slate-800 text-blue-400'
            : 'text-slate-400 hover:bg-slate-800 hover:text-slate-100') %>">
      <span>Log activity</span>
    </a>
  </nav>

  <div class="mt-auto pt-6 border-t border-slate-800 text-xs text-slate-600">
    Prototype • Sample data only
  </div>
</div>