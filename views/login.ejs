<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Login | FlowTrack</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-slate-950 text-slate-100">

<div class="flex min-h-screen">
  <!-- Left side: form -->
  <div class="flex-1 flex items-center justify-center px-6 py-10">
    <div class="w-full max-w-md">
      <!-- Brand -->
      <div class="mb-8 flex items-center gap-3">
        <div class="h-9 w-9 rounded-2xl bg-gradient-to-tr from-blue-500 to-cyan-400 flex items-center justify-center text-xs font-bold text-slate-950 shadow-lg shadow-blue-500/30">
          FT
        </div>
        <div>
          <div class="text-sm font-semibold text-slate-100">
            FlowTrack
          </div>
          <div class="text-xs text-slate-500">
            See when you work best
          </div>
        </div>
      </div>

      <h1 class="text-2xl font-semibold tracking-tight text-slate-50">
        Welcome back
      </h1>
      <p class="mt-2 text-sm text-slate-400">
        This is a demo experience. Sign in to explore your effectiveness
        dashboard and logging flow.
      </p>

      <form
        class="mt-8 space-y-5"
        action="/dashboard"
        method="get"
      >
        <div class="space-y-2">
          <label for="email" class="block text-xs font-medium text-slate-300">
            Email
          </label>
          <input
            id="email"
            name="email"
            type="email"
            autocomplete="email"
            placeholder="you@example.com"
            class="w-full rounded-xl bg-slate-950 border border-slate-700 px-3 py-2.5 text-sm text-slate-100 placeholder:text-slate-500 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
          />
        </div>

        <div class="space-y-2">
          <label for="password" class="block text-xs font-medium text-slate-300">
            Password
          </label>
          <input
            id="password"
            name="password"
            type="password"
            autocomplete="current-password"
            placeholder="••••••••"
            class="w-full rounded-xl bg-slate-950 border border-slate-700 px-3 py-2.5 text-sm text-slate-100 placeholder:text-slate-500 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
          />
        </div>

        <p class="text-[11px] text-slate-500">
          This prototype does not use real authentication. Any email and password
          will take you to the dashboard.
        </p>

        <button
          type="submit"
          class="mt-2 inline-flex w-full items-center justify-center rounded-xl bg-blue-500 px-4 py-2.5 text-sm font-semibold text-slate-950 shadow-sm hover:bg-blue-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 focus:ring-offset-slate-950"
        >
          Sign in
        </button>
      </form>
    </div>
  </div>

  <!-- Right side: visual panel -->
  <div class="hidden lg:flex lg:flex-1 bg-gradient-to-br from-slate-900 via-slate-950 to-slate-950 border-l border-slate-800">
    <div class="relative flex flex-1 items-center justify-center px-10">
      <div class="max-w-md">
        <div class="inline-flex items-center gap-2 rounded-full bg-slate-900/60 border border-slate-700 px-3 py-1 mb-4">
          <span class="h-1.5 w-1.5 rounded-full bg-emerald-400"></span>
          <span class="text-[11px] text-slate-300">
            Prototype • Sample data only
          </span>
        </div>

        <h2 class="text-xl font-semibold text-slate-50">
          Discover your real peak hours.
        </h2>
        <p class="mt-2 text-sm text-slate-400">
          Log a few days of work and FlowTrack highlights the times of day
          and days of the week where you naturally get the most done.
        </p>

        <div class="mt-6 grid grid-cols-2 gap-3 text-xs">
          <div class="rounded-2xl bg-slate-900/70 border border-slate-800 p-3">
            <div class="text-[11px] text-slate-400">
              Peak time
            </div>
            <div class="mt-2 text-lg font-semibold text-blue-400">
              9 AM
            </div>
            <div class="mt-1 text-[11px] text-slate-500">
              highest average focus
            </div>
          </div>
          <div class="rounded-2xl bg-slate-900/70 border border-slate-800 p-3">
            <div class="text-[11px] text-slate-400">
              Avg effectiveness
            </div>
            <div class="mt-2 text-lg font-semibold text-slate-50">
              7.8/10
            </div>
            <div class="mt-1 text-[11px] text-slate-500">
              across logged sessions
            </div>
          </div>
        </div>

        <div class="mt-4 rounded-2xl bg-slate-900/50 border border-dashed border-slate-700 p-3">
          <div class="h-[72px] w-full rounded-xl bg-gradient-to-tr from-blue-500/15 via-cyan-400/10 to-sky-500/5 flex items-center justify-center text-[11px] text-slate-400">
            Preview of your dashboard charts
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

</body>
</html>